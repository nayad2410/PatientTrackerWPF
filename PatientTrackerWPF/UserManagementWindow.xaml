<Window x:Class="PatientTrackerWPF.UserManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="User Account Management" 
        Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="User Account Management" 
                   FontSize="24" FontWeight="Bold" 
                   Foreground="#2B5F75" Margin="0,0,0,20"/>

        <!-- Add New User Section -->
        <Border Grid.Row="1" Background="White" 
                BorderBrush="#DDD" BorderThickness="1" 
                CornerRadius="5" Padding="15" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Add New User" FontSize="16" FontWeight="Bold" 
                          Foreground="#2B5F75" Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Username" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="UsernameBox" Height="25"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,10,0">
                        <TextBlock Text="Full Name" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="FullNameBox" Height="25"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Margin="0,0,10,0">
                        <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="EmailBox" Height="25"/>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Margin="0,0,10,0">
                        <TextBlock Text="Role" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="RoleComboBox" Height="25">
                            <ComboBoxItem>Admin</ComboBoxItem>
                            <ComboBoxItem>Doctor</ComboBoxItem>
                            <ComboBoxItem>Nurse</ComboBoxItem>
                            <ComboBoxItem>Technician</ComboBoxItem>
                            <ComboBoxItem>Researcher</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>

                    <Button Grid.Column="4" Content="Add User" 
                            Click="AddUser_Click"
                            Height="30" Width="100" 
                            Background="#2B5F75" Foreground="White"
                            FontWeight="Bold" VerticalAlignment="Bottom"/>
                </Grid>

                <TextBlock Text="* Default password will be: Welcome123! (User must change on first login)" 
                          FontSize="10" Foreground="Gray" Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- Users List -->
        <Border Grid.Row="2" Background="White" 
                BorderBrush="#DDD" BorderThickness="1" 
                CornerRadius="5" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Existing Users" 
                          FontSize="16" FontWeight="Bold" 
                          Foreground="#2B5F75" Margin="0,0,0,10"/>

                <DataGrid Grid.Row="1" x:Name="UsersGrid"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          Background="White"
                          BorderBrush="#DDD">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Username" 
                                          Binding="{Binding Username}" 
                                          Width="120" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Full Name" 
                                          Binding="{Binding FullName}" 
                                          Width="200" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Email" 
                                          Binding="{Binding Email}" 
                                          Width="200" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Role" 
                                          Binding="{Binding Role}" 
                                          Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Created" 
                                          Binding="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd}}" 
                                          Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Last Login" 
                                          Binding="{Binding LastLogin, StringFormat={}{0:yyyy-MM-dd HH:mm}}" 
                                          Width="150" IsReadOnly="True"/>
                        <DataGridTemplateColumn Header="Actions" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Reset Password" 
                                                Click="ResetPassword_Click"
                                                Tag="{Binding}"
                                                Margin="2" Padding="5,2"
                                                Background="Orange" Foreground="White"/>
                                        <Button Content="Delete" 
                                                Click="DeleteUser_Click"
                                                Tag="{Binding}"
                                                Margin="2" Padding="5,2"
                                                Background="DarkRed" Foreground="White"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Refresh" Click="RefreshUsers_Click"
                    Width="100" Height="30" Margin="5"
                    Background="#2B5F75" Foreground="White" FontWeight="Bold"/>
            <Button Content="Close" Click="Close_Click"
                    Width="100" Height="30" Margin="5"
                    Background="Gray" Foreground="White" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Window>