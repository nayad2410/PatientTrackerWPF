<Application x:Class="PatientTrackerWPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="LoginWindow.xaml">
    <Application.Resources>

        <!-- App-wide styles -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="DarkBlue"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
         
        </Style>

        <!-- Logo and branding resources -->
        <SolidColorBrush x:Key="BrandPrimaryColor" Color="#2E86AB"/>
        <SolidColorBrush x:Key="BrandSecondaryColor" Color="#A23B72"/>
        <SolidColorBrush x:Key="BrandAccentColor" Color="#F18F01"/>

    </Application.Resources>
</Application>